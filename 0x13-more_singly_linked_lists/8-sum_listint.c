#include "lists.h"

/**
 * sum_listint - returns sum of all the data in the listint_t list
 * @head: input value
 *
 * Return: return all sum otherwise, 0
 */

int sum_listint(listint_t *head)
{
	int plus = 0;
	listint_t *dail = head;

	while (dail)
	{
		plus += dail->n;
		temp = temp->next;
	}
	return (sum);
}
